// Function Declaration--------->

/* Function => The concept of functions came into existence to support the way we organize and
structure code. A function is a block of code that performs a specific task and can
be reused throughout the program. */


//Introduction to function declaration =>

function functionName(){ //declaration
    //function body
  }
  
  functionName(); //calling function
  
  
  /* Parameters , Arguments and return statement.=>A parameter is a variable that is declared within the parentheses of the function declaration It serves as a
  placeholder or a reference for the input value that will be passed to the function when it is called.*/
  
  /* // Syntax
  
  function functionName(parameter_1, Parameter_2){ // function declaration
    //function body
  }
  
  functionName(argument_1,argument_2); //calling function
  
  */
  
  
  
  /* The return statement is used to output a value from a function. The return statement terminates the
  execution of the function. It also specifies the value that should be returned to the calling code. The value
  returned by the function can be assigned to a variable or used in an expression. */
  
  /*
  
  // Function declaration
  function functionName(parameter1, parameter2) {
  // function body
  return return_value;
  }
  // Calling a function
  let result = functionName(argument1, argument2);
  // The value of return_value will be stored in the result
  
  */
  